From folder containing src folder (workspace):
- Run ```colcon build``` in a terminal
- Preferably on a new terminal but same workspace, ```source install/setup.bash && ros2 run midterm_drive pubsub```
